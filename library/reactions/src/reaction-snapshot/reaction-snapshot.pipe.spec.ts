import { ReactionSnapshotPipe } from './reaction-snapshot.pipe';

describe('ReactionSnapshotPipe', () => {
  it('create an instance', () => {
    const pipe = new ReactionSnapshotPipe();
    expect(pipe).toBeTruthy();
  });
});
