import {ReactionsDirective} from './reactions.directive';

describe(ReactionsDirective.name, () => {
    it('should create an instance', () => {
        const directive = new ReactionsDirective();
        expect(directive).toBeTruthy();
    });
});
